#include <stdio.h>
#include "main.h"
/**
 * main - entry point
 * Description: personal tester code for functions
 * Return: always 0 (success)
 */
int main(void)
{
	print_times_table(0);
	_putchar('\n');
	print_times_table(5);
	_putchar('\n');
	print_times_table(12);
	_putchar('\n');

	return (0);
}
