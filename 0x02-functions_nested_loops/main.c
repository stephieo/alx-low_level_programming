#include <stdio.h>
#include "main.h"
/**
 * main - entry point
 * Description: personal tester code for functions
 * Return: always 0 (success)
 */
int main(void)
{
	int n;
	n = add(10, 48);
	printf("sum is %d\n", n);
	return (0);
}
